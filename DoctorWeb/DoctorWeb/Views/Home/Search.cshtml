@model IEnumerable<DoctorWeb.Models.CustomModels.PatientSearch>
@{
    ViewBag.Title = "Search";
    //Layout = "~/Views/Shared/_Layout.cshtml";
    Layout = "~/Views/Shared/_LightBoxLayout.cshtml";
}

@*<script src="~/Content/js/jquery-1.11.1.min.js"></script>
        
<script src="~/Content/js/jquery-ui.min.js"></script>

*@

@*<h2>Search</h2>*@

<table class="table" id="SearchRecords">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.No)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RefferalName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DepartmentName)
        </th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.No)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RefferalName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DepartmentName)
            </td>
            <td>
                @Html.ActionLink("Select", "Edit", new { id = item.No })
            </td>
        </tr>
    }
</table>

@*<script type="text/javascript">
    $(function () {
        $("#SearchRecords").dialog({
            autoOpen: true,
            title: 'Add User Form',
            modal: true,
            open: function () {
                var patientname = document.getElementById("PatientName").value;
                abhay(this).load('@Url.Action("Search", "Home", new { PatientName = "ID"})'.replace("ID", patientname));
            },
        });
    });
</script>*@